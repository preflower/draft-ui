# Draft UI

A shadcn-like component library for Vue and React.

- **Monorepo**: Built with pnpm and monorepo.
- **Copy-Paste**: Import components directly into your project files.
- **Multi-Framework**: Supports both Vue and React.
- **CLI Powered**: Use `draft-cli` to manage components.
  